syntax = "proto3";

package users;

service ProtoService {
  rpc GetUser () returns (User);
  rpc GetUsers () returns (Users);
}

message User {
  string name = 1;
  string lastName = 2;
}

message Users {
  repeated User users = 1;
}
